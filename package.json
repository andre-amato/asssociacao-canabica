{
  "name": "fazenda-canabica",
  "packageManager": "npm@10.8.2",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "start": "turbo run start"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
